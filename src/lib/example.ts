/**
 * Example utility function to verify TypeScript configuration
 * This file demonstrates strict type checking and can be removed later
 */

export function add(a: number, b: number): number {
  return a + b;
}

export function greet(name: string): string {
  return `Hello, ${name}!`;
}
