# Fizzy Ralph Progress Log

## Codebase Patterns

- Pattern: EAS Build configuration uses three profiles: development (with simulator support), preview (internal distribution), and production (store-ready builds)
- Pattern: iOS and Android use consistent bundle identifier: `com.languageden.app`
- Pattern: app.json must include expo-router plugin and enable typed routes experiment
- Pattern: EAS CLI is available via npx, no need to install as dependency
- Pattern: Use .npmrc with `engine-strict=true` and `node-linker=hoisted` for pnpm configuration
- Pattern: All npm scripts in package.json should be organized by category (dev, testing, quality, builds)
- Pattern: Always run format before committing to ensure code consistency
- Pattern: Use `React.JSX.Element` instead of `JSX.Element` for component return types to avoid ESLint deprecation warnings
- Pattern: Use `npx pnpm add` to install dependencies when pnpm is configured (not `npx expo install`)
- Pattern: Tamagui configuration uses `defaultConfig` from `@tamagui/config/v4` (not `config`)
- Pattern: TamaguiProvider wraps the root layout to provide theme and tokens globally
- Pattern: Add eslint-disable comment for no-empty-object-type on TamaguiCustomConfig interface

---

Board ID: 03fe112po6gokbkt0pzn2sft2
Started: Tue Jan 13 05:44:38 AM +07 2026

---

## [Mon Jan 13 05:45:00 AM +07 2026] - Card #47

- Created app.json with Expo configuration including iOS bundle identifier and Android package name
- Created eas.json with three build profiles (development, preview, production)
- Verified EAS CLI is available via npx
- Confirmed type checking and linting pass

**Files changed:**
- app.json (created) - Expo configuration with platform-specific settings
- eas.json (created) - EAS Build profiles for all platforms
- AGENTS.md (created) - Documentation of EAS Build patterns

**Learnings for future iterations:**
- EAS Build requires app.json with platform-specific identifiers (bundleIdentifier for iOS, package for Android)
- eas.json build profiles support different configurations per environment (dev, preview, prod)
- Development builds can target simulators/emulators, but production builds cannot
- Android development uses APK format for faster iteration; production uses AAB for Play Store
- EAS CLI is available globally via npx - no need to add as project dependency
- Quality gates at this stage: type-check and lint (tests not yet configured)

---

## [Tue Jan 13 05:54:30 AM +07 2026] - Card #46

- Created .npmrc configuration file to enforce pnpm as the package manager
- Configured node-linker=hoisted for React Native Metro bundler compatibility
- Verified all npm scripts were already present in package.json (dev, test, type-check, lint, format, build commands)
- Ran format to ensure code consistency across the project
- Confirmed all quality gates pass (type-check, lint, format:check)

**Files changed:**

- .npmrc (created) - pnpm configuration with engine enforcement and hoisted node linking
- AGENTS.md (updated) - Added package manager configuration patterns
- specs/progress.txt (updated) - Updated codebase patterns and added this entry

**Learnings for future iterations:**

- React Native Metro bundler requires pnpm to use hoisted node-linker to properly resolve dependencies
- Setting engine-strict=true in .npmrc enforces the package manager specified in package.json
- When running pnpm via npx, npm warnings about unknown pnpm-specific config keys are expected and harmless
- The package.json packageManager field should specify exact pnpm version (e.g., pnpm@8.15.0)
- Format check should be run alongside type-check and lint as part of quality gates
- npm scripts should be logically grouped: development (dev/web/ios/android), testing (test/test:watch), quality (type-check/lint/format), builds (build:web/ios/android)

---

## [Tue Jan 13 06:00:15 AM +07 2026] - Card #45

- Installed expo-router and required dependencies (react-native-safe-area-context, react-native-screens, expo-linking, expo-constants, expo-status-bar)
- Created app/_layout.tsx with Stack navigation layout and headerShown: false
- Created app/index.tsx with basic "Language Den" title screen using React Native components
- Verified app.json already had expo-router plugin, scheme, and typedRoutes experiment configured
- All quality gates pass (type-check, lint, format:check)

**Files changed:**
- package.json (updated) - Added expo-router and dependencies
- app/_layout.tsx (created) - Root layout with Stack navigation
- app/index.tsx (created) - Root route with title screen
- AGENTS.md (updated) - Added expo-router setup patterns

**Learnings for future iterations:**
- Use `npx pnpm add` to install packages when pnpm is configured via .npmrc, not `npx expo install` which fails to find pnpm
- Component return types should use `React.JSX.Element` instead of `JSX.Element` to avoid ESLint deprecation warnings
- expo-router requires specific dependencies: react-native-safe-area-context, react-native-screens, expo-linking, expo-constants, expo-status-bar
- The app/_layout.tsx file is the root layout and uses Stack from expo-router for navigation
- Set `screenOptions={{ headerShown: false }}` to hide navigation headers by default
- app.json configuration for expo-router was already done in Card #47 (plugins, scheme, typedRoutes)

---

## [Tue Jan 13 06:06:00 AM +07 2026] - Card #50

- Installed Tamagui dependencies (`tamagui` and `@tamagui/config`)
- Created tamagui.config.ts using v4 defaultConfig with type declarations
- Updated app/_layout.tsx to wrap app with TamaguiProvider
- All quality gates pass (type-check, lint, format:check)

**Files changed:**
- package.json (updated) - Added Tamagui dependencies
- tamagui.config.ts (created) - Tamagui configuration with v4 defaultConfig
- app/_layout.tsx (updated) - Wrapped with TamaguiProvider
- AGENTS.md (updated) - Added Tamagui setup patterns

**Learnings for future iterations:**
- Use `defaultConfig` from `@tamagui/config/v4`, not `config` (which doesn't exist)
- The v4 config provides complete token system out of the box (colors, spacing, sizes, fonts, themes)
- TamaguiProvider must wrap the root layout to make tokens/themes available globally
- Add `eslint-disable-next-line @typescript-eslint/no-empty-object-type` comment on TamaguiCustomConfig interface to avoid ESLint errors
- Module augmentation pattern is required: `declare module 'tamagui' { interface TamaguiCustomConfig extends AppConfig {} }`
- Import tamagui.config from relative path in app/_layout.tsx: `import config from '../tamagui.config'`

---
---

**Reached max iterations with 14 card(s) remaining**
