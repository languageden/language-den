# Fizzy Ralph Progress Log

## Codebase Patterns

### Tamagui Token System

- Use `createTokens()` from `tamagui` package to add custom color tokens to Tamagui config
- Custom colors should use semantic naming with standard scale increments (50-950)
- The `defaultConfig.tokens` from v4 includes `radius`, `zIndex`, `space`, and `size` but NOT `color`
- Import `createTokens` from `tamagui`, not from `@tamagui/config/v4`
- Spacing tokens use 4px base unit (1 = 4px) for consistent layout rhythm
- Typography tokens follow modular scale ratio of 1.25 (major third) starting from 16px base
- Line height tokens provide optimal readability for different text sizes
- Size tokens should match spacing tokens for consistency across components

### Tamagui Theme System

- Define separate `light` and `dark` theme objects with semantic color mappings
- Map semantic names (background, color, primary, etc.) to token values using `tokens.color.tokenName`
- Theme colors are organized into categories: backgrounds, text, borders, shadows, interactive, feedback
- Light theme uses light backgrounds with dark text; dark theme uses dark backgrounds with light text
- Include interactive state variants: base, hover, press, and focus for each color
- Tamagui transforms token references into Variable objects with `val`, `key`, `name`, `variable` properties
- Access actual color values in tests via `theme.colorName.val`

### Tamagui Animation System

- Tamagui v4 uses CSS-based animation strings: `"cubic-bezier(x1, y1, x2, y2) duration"` format
- Extend `defaultConfig.animations` by spreading it and overriding the nested `animations` property
- There is NO `createAnimations()` function - use plain object with CSS animation strings
- Access animations via `config.animations.animations` (nested structure)
- Keep standard animations under 400ms for responsive feel; use 500ms+ for celebration animations
- Use consistent easing: `cubic-bezier(0.4, 0.0, 0.2, 1)` for standard, bouncy curve for celebrations

### Tamagui Styled Components

- Use `styled(YStack, { ... })` to create reusable UI component primitives
- Some style properties (like `borderRadius`) must be in variants, not base config due to TypeScript constraints
- Styled components are objects, not functions, but work with React.createElement()
- Define interactive states at same level as base styles: `hoverStyle`, `pressStyle`, `focusStyle`
- Use variants pattern for conditional styling: `variants: { prop: { value: { styles } } }` + `defaultVariants`
- Testing styled components requires matchMedia mock in test/setup.ts for Tamagui's media query support

### Dashboard Layout Patterns

- Grid layouts use nested XStack (rows) within YStack with equal `flex={1}` on grid cells for responsive equal widths
- Wrap grid children in YStack with `flex={1}` to ensure proper flex behavior in XStack rows
- Use consistent `gap="$3"` for spacing between grid cells for balanced layouts
- Dashboard sections can omit Card wrapper when they don't need unified container styling (e.g., when composing multiple Cards)
- Section headers use Text with `fontSize="$5"` and `fontWeight="600"` to label major content areas

### Tamagui Component Props

- Use `width` and `height` for sizing (not shorthands `w`/`h`) on YStack/XStack components
- Properties like `borderRadius` cannot be used inline on YStack/XStack - must be in styled component variants
- Use `flex` property (not shorthand `f`) for flex layout properties
- Tamagui v4 has `onlyAllowShorthands: true` which restricts available inline properties
- When creating function components that wrap Tamagui primitives, avoid dynamic style props that TypeScript rejects
- Text-based styled components (like Button) don't support: `textAlign`, `lineHeight`, `borderRadius` (use variants), `userSelect`, `minWidth`
- Text components do NOT support `textAlign` - wrap Text in YStack with `items="center"` for centered text
- Text components do NOT support `minWidth` - wrap Text in YStack with fixed `width` for minimum width
- Dynamic `color` prop (string type) not allowed on Text - use semantic tokens only (`$color`, `$secondary`, etc.)
- Use `aria-label` and `role` for accessibility instead of deprecated `accessibilityLabel` and `accessibilityHint`

### TypeScript Type Definitions

- Group related interfaces in a single file (e.g., dashboard.ts for all dashboard types)
- Use enum for discriminated union types with snake_case string values for API compatibility
- Use `Date` type for timestamps (serialized to/from ISO strings in API calls)
- Use `Record<string, unknown>` for flexible metadata objects
- Nest related data in structured objects rather than flat properties
- Use JSDoc comments to document interface purpose

### TypeScript Import Best Practices

- Use `import type` for type-only imports to satisfy ESLint rule `@typescript-eslint/consistent-type-imports`
- Separate value imports (enums, functions) from type imports
- Remove trivially inferred type annotations (rule: `@typescript-eslint/no-inferrable-types`)
- Let TypeScript infer parameter types from default values (e.g., `greeting = 'Good morning'`)
- Use `.toString()` when interpolating numbers in template literals to satisfy `@typescript-eslint/restrict-template-expressions`

### Mock Data Services

- Export both synchronous and asynchronous versions for flexibility
- Define mock data as module-level constants for consistency
- Async functions should accept delay parameter (default 500ms) to simulate network latency
- Mock data should include all enum variants for comprehensive testing
- Use JSDoc comments with `@example` tags to document usage patterns

### Domain Layer Pure Functions

- Domain functions must be pure TypeScript with no React imports or side effects
- Functions that depend on time should accept optional Date parameter (default `new Date()`) for testability
- Use `Date.getHours()` to extract hour in local timezone for time-based logic
- Export interfaces for complex return types to enable type reuse
- Use `@vitest-environment node` for domain tests to avoid DOM overhead
- Document time ranges and behavior clearly in JSDoc comments
- Test all boundaries, edge cases, default behavior, and consistency

### Dashboard Section Components

- Dashboard sections are functional components that compose Card and other UI primitives (not styled components)
- Export props interface for each section component for external type reuse
- Use Card as outer container with custom padding (e.g., `p="$6"`) for consistent dashboard styling
- Use `gap` props on YStack/XStack for spacing between elements within sections
- For prominent headings, use H2 with explicit fontSize and fontWeight
- Icons should use large font sizes for visual hierarchy (e.g., `fontSize="$8"` for main icon)
- Dashboard sections accept callback props (e.g., `onStartReview: () => void`) for user interactions
- Test with React.createElement pattern to verify prop acceptance without full DOM rendering

### React Custom Hooks

- Custom hooks start with `use` prefix and are placed in `src/hooks/` directory
- Hooks return objects with named properties (not arrays) for clarity: `{ data, isLoading, error }`
- Use explicit return type annotations on hook functions for type safety
- For async data fetching: initialize loading=true, data=null, error=null, then update in useEffect
- Call async functions in useEffect with `void` operator to satisfy ESLint no-floating-promises rule
- Empty dependency array `[]` ensures effect runs only once on mount
- Always use try-catch-finally for async operations: set loading in finally block
- Testing hooks requires React context - test the underlying service layer and type structure instead
- React hooks using React Native dependencies cannot be tested with renderHook due to Flow type syntax issues

---

Board ID: 03fe9lj5hgoq1bllqhi2fsabi
Started: Tue Jan 13 10:05:35 PM +07 2026

---

## [Tue Jan 13 10:14:35 PM +0700 2026] - Card #83

- Implemented spacing tokens in tamagui.config.ts using 4px base unit for consistent layout rhythm
- Defined typography tokens (fontSize and lineHeight) following modular scale ratio of 1.25 starting from 16px base
- Spacing scale includes fractional values (0.5, 1.5, 2.5) for fine control and large values (12, 16, 24, 32) for major sections
- Typography scale ranges from 12px (xs) to 64px (6xl) with semantic names for different use cases
- Line height tokens (tight, snug, normal, relaxed, loose) optimized for readability at different text sizes
- Size tokens mirror spacing tokens for consistency across width/height and spacing properties
- Created comprehensive tests in tamagui.config.test.ts to verify spacing, typography, and size tokens
- Files changed: tamagui.config.ts, tamagui.config.test.ts, specs/progress.txt

**Learnings for future iterations:**

- Spacing and size tokens should use the same values to maintain consistency between width/height and padding/margin
- The 4px base unit provides precise control while maintaining 8px grid alignment for major spacing values
- Typography scale using modular scale ratio creates harmonious visual hierarchy without arbitrary sizes
- Line heights should be optimized for each text size category: tight for headings, normal (1.5x) for body text, loose for display
- Font sizes should be documented with semantic names (xs, sm, base, lg, xl, 2xl, etc.) in comments for clarity
- Fractional spacing values (0.5, 1.5, 2.5) are essential for fine-tuning component layouts and alignment
- Token values can be accessed in tests via `.val` property (e.g., `tokens.space[4].val`) just like theme colors
- Custom tokens are merged with defaultConfig.tokens using spread operator to preserve default radius, zIndex, etc.

---

## [Tue Jan 13 10:12:10 PM +0700 2026] - Card #82

- Implemented semantic color mappings for light and dark mode themes in tamagui.config.ts
- Created comprehensive theme objects with semantic names: background, color, borders, shadows, primary, secondary, success, warning, error
- Added interactive state variants (hover, press, focus) for all color categories
- Light theme uses neutral50 background with neutral900 text; dark theme uses neutral950 background with neutral50 text
- Created comprehensive tests in tamagui.config.test.ts to verify theme configuration
- Files changed: tamagui.config.ts, tamagui.config.test.ts, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Theme objects map semantic property names to token values using `tokens.color.tokenName` syntax
- Tamagui automatically transforms token references into Variable objects with properties: `val`, `key`, `name`, `variable`
- To test theme colors, access the actual hex value via `theme.colorName.val` (e.g., `lightTheme.background.val`)
- Interactive states should follow a consistent pattern: lighter shades for hover/press in dark mode vs darker shades in light mode
- Border focus colors should use the primary color (primary500) in both themes for consistency
- Organize theme colors into logical categories with inline comments: backgrounds, text, borders, shadows, interactive, feedback
- Include transparent variants for backgrounds and text colors to support overlay and fade effects
- The `themes` object is passed to `createTamagui()` alongside `tokens` to configure both color tokens and semantic theme mappings

---

## [Tue Jan 13 10:07:50 PM +07 2026] - Card #81

- Defined custom color tokens for Language Den brand in tamagui.config.ts
- Added 5 semantic color scales: primary, success, warning, error, and neutral
- Each scale includes 11 shades (50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950)
- Implemented color tokens using `createTokens()` from tamagui package
- Files changed: tamagui.config.ts, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- The `createTokens` function must be imported from `tamagui`, not from `@tamagui/config/v4`
- The `defaultConfig.tokens` object does not include a `color` property by default - it only has `radius`, `zIndex`, `space`, and `size`
- Colors should be added to the tokens object (not themes), and themes will reference these color tokens
- Standard design system color scales use increments: 50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950 (where 500 is typically the "main" color)
- Semantic color naming (primary, success, warning, error, neutral) is preferred over generic names (blue, green, yellow, red, gray)
- Custom tokens automatically get TypeScript typing through the `TamaguiCustomConfig` module declaration

---
## [Mon Jan 13 10:18:45 PM +07 2026] - Card #84

- Implemented animation timing tokens in tamagui.config.ts for consistent motion across the app
- Added four animation presets: quick (150ms), normal (250ms), slow (400ms), and celebration (500ms)
- Used CSS-based animation strings with cubic-bezier easing functions
- Quick/normal/slow use standard easeInOut curve; celebration uses bouncy curve for achievements
- Extended defaultConfig.animations by spreading and overriding the nested animations property
- Created comprehensive tests in tamagui.config.test.ts to verify animation timing and easing
- All animation tests pass including duration verification and easing function validation
- Files changed: tamagui.config.ts, tamagui.config.test.ts, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Tamagui v4 does NOT have a `createAnimations()` function - the card's example code was incorrect
- Animations in Tamagui v4 are CSS strings in format: `"cubic-bezier(x1, y1, x2, y2) duration"`
- Must extend `defaultConfig.animations` by spreading entire object and overriding nested `animations` property
- Animation structure is nested: access via `config.animations.animations.quick`, not `config.animations.quick`
- Testing animations requires parsing CSS strings with regex: `/(\d+)ms/.exec(animationString)`
- Use optional chaining (`?.[1]`) when accessing regex match groups to satisfy TypeScript strict mode
- ESLint prefers `RegExp.exec()` over `String.match()` for performance and consistency
- Standard animations kept under 400ms for responsive feel; celebration animation can be longer (500ms+)
- Reduced motion support should be implemented in components using `useReducedMotion()` hook from React Native
- The cubic-bezier values define the animation curve: (0.4, 0.0, 0.2, 1) = easeInOut, (0.68, -0.55, 0.265, 1.55) = bouncy

---

## [Mon Jan 13 10:24:45 PM +07 2026] - Card #85

- Created reusable Card component in src/ui/Card.tsx using Tamagui's styled(YStack)
- Card provides consistent visual container with elevation, rounded corners, and theme-aware styling
- Implemented interactive states: hoverStyle, pressStyle, and focusStyle for accessibility
- Used variants system for borderRadius styling (borderRadius in base config causes TypeScript errors)
- Created comprehensive tests in src/ui/Card.test.tsx verifying component creation and prop acceptance
- Added matchMedia mock to test/setup.ts for Tamagui components that use media queries
- All quality gates pass: type-check, lint, and test (43 tests passing)
- Files changed: src/ui/Card.tsx, src/ui/Card.test.tsx, test/setup.ts, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Tamagui styled components are objects (not plain functions), but work with React.createElement()
- Some style properties like borderRadius must be defined in variants, not in base style config
- The `borderWidth` property doesn't have a shorthand (`bw`) that TypeScript recognizes - use full name
- Interactive states (hoverStyle, pressStyle, focusStyle) should be defined at the same level as base styles
- matchMedia mock is required in test/setup.ts for Tamagui components that use @tamagui/select or similar
- Use `globalThis as any` with appropriate eslint-disable comments to mock window.matchMedia in strict mode
- Variants pattern: `variants: { propName: { value: { styles } } }` with `defaultVariants: { propName: value }`
- Styled component tests don't need full DOM rendering - React.createElement() is sufficient for prop testing
- The animation prop accepts token names like 'quick', 'normal', 'slow' defined in tamagui.config.ts
- Shadow props work cross-platform: shadowColor, shadowOffset, shadowOpacity, shadowRadius (iOS/web) + elevation (Android)

---

## [Mon Jan 13 10:28:45 PM +07 2026] - Card #86

- Created MetricCard component in src/ui/MetricCard.tsx for displaying dashboard metrics
- Component displays label, prominent value, optional icon, and optional trend indicator
- Built on top of Card component for consistent elevation and styling
- Implemented trend indicator with up/down arrows and color-coded text (success/error)
- Icon container uses primary color background with fixed size (40px x 40px)
- Created comprehensive tests in src/ui/MetricCard.test.tsx (11 tests)
- All quality gates pass: type-check, lint, and test (54 tests passing)
- Files changed: src/ui/MetricCard.tsx, src/ui/MetricCard.test.tsx, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Tamagui v4 requires `width` and `height` properties (not shorthands `w`/`h`) for YStack/XStack sizing
- The `flex` property must be used instead of shorthand `f` for flex layout
- Properties like `borderRadius` cannot be used inline on YStack/XStack - they must be in styled component variants
- Tamagui v4 has `onlyAllowShorthands: true` setting which restricts which properties can be used directly in JSX
- Dynamic color props (e.g., `color?: string`) cannot be passed directly to `bg` prop due to TypeScript type constraints
- When TypeScript rejects a style prop, either use a fixed token value or create a styled component with variants
- For simple icon containers, omitting borderRadius entirely is acceptable if rounded corners aren't critical
- Function components wrapping Tamagui primitives should avoid dynamic style props that TypeScript cannot validate
- MetricCard test pattern: verify all props render correctly, test optional props independently, test combined props
- Component should use semantic color tokens ($primary, $success, $error) rather than hardcoded colors

---

## [Tue Jan 13 10:33:00 PM +0700 2026] - Card #87

- Created Button component in src/ui/Button.tsx using Tamagui's styled(Text)
- Button provides accessible, cross-platform button with primary styling and press feedback
- Implemented interactive states: hoverStyle, pressStyle, and focusStyle for accessibility
- Used variants system for borderRadius styling (must be in variants, not base config)
- Shadow properties work cross-platform: shadowColor, shadowOffset, shadowOpacity, shadowRadius + elevation
- Created comprehensive tests in src/ui/Button.test.tsx (15 tests)
- All quality gates pass: type-check, lint, and test (69 tests passing)
- Files changed: src/ui/Button.tsx, src/ui/Button.test.tsx, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Text-based styled components have more restrictions than YStack/XStack components
- Properties that don't work on styled(Text): textAlign, lineHeight, borderRadius (base), userSelect
- borderRadius must be defined in variants for Text-based components, just like YStack/XStack
- Button components work well when styled from Text with proper padding, background, and interactive states
- Use aria-label instead of deprecated accessibilityLabel for accessibility support
- Use role prop instead of deprecated accessibilityHint for accessibility
- Text components automatically handle typography styling (fontSize, fontWeight, color)
- cursor: 'pointer' is allowed on Text components for proper web cursor behavior
- Scale transform in pressStyle (scale: 0.98) provides nice tactile feedback on press
- All styled components (Text, YStack, XStack) share the same pattern: variants with defaultVariants

---

## [Tue Jan 13 10:35:50 PM +0700 2026] - Card #88

- Created ActivityItem component in src/ui/ActivityItem.tsx for displaying individual activity feed items
- Component displays icon, description, and relative timestamp in horizontal layout
- Icon container uses fixed size (40px x 40px) for consistent alignment across different activity types
- Description and timestamp use proper typography hierarchy with semantic colors
- Implemented proper spacing: vertical padding for list items, gaps for icon/content separation
- Created comprehensive tests in src/ui/ActivityItem.test.tsx (13 tests)
- All quality gates pass: type-check, lint, and test (82 tests passing)
- Files changed: src/ui/ActivityItem.tsx, src/ui/ActivityItem.test.tsx, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- ActivityItem uses XStack for horizontal layout with icon and content side-by-side, unlike MetricCard which uses more complex nesting
- Icon container should use fixed width/height (`width="$10" height="$10"`) for consistent alignment, same pattern as MetricCard
- Use `flex={1}` on content container (YStack) to allow text content to fill remaining horizontal space
- Description text uses `fontSize="$3"` with `fontWeight="500"` for readability, larger than timestamp
- Timestamp uses `fontSize="$2"` with `$secondary` color for visual hierarchy, indicating less important information
- Vertical gap between description and timestamp should be tight (`gap="$1"`) since they're closely related
- Horizontal gap between icon and content should be comfortable (`gap="$3"`) for visual separation
- Apply vertical padding (`py="$3"`) directly to ActivityItem for proper spacing when used in a list
- Component accepts icon as ReactNode prop for maximum flexibility - can be SVG, emoji, or any React element
- Use semantic colors (`$color`, `$secondary`) rather than hardcoded colors for theme-aware styling
- ActivityItem is a pure presentational component with no state or interactivity - simpler than Button or Card
- Testing pattern remains consistent: verify component definition, prop acceptance, and element creation

---
## [Tue Jan 13 10:37:45 PM +0700 2026] - Card #95

- Created TypeScript interfaces for all dashboard data structures in src/types/dashboard.ts
- Defined UserInfo interface for user information (id, name, email, avatarUrl)
- Defined LearningStats interface for tracking cards learned, streak, reviews, and accuracy
- Defined ReviewQueue interface with breakdown by urgency (overdue, dueToday, upcoming, total)
- Defined ActivityType enum for discriminated union (card_learned, streak_milestone, deck_completed, perfect_review)
- Defined Activity interface for individual feed entries with type, description, timestamp, metadata
- Defined DashboardData interface as the complete dashboard data structure with all nested types
- Created AGENTS.md in src/types/ to document type definition patterns and conventions
- All quality gates pass: type-check, lint, and test (82 tests passing)
- Files changed: src/types/dashboard.ts, src/types/AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Pattern: Group related interfaces in a single file for cohesion (e.g., dashboard.ts for all dashboard types)
- Pattern: Export all interfaces and enums for use across the application
- Pattern: Use JSDoc comments to document the purpose of each interface
- Pattern: Use enum for discriminated union types with snake_case string values for API compatibility
- Pattern: Use `Date` type for timestamps (will be serialized to/from ISO strings in API calls)
- Pattern: Use `Record<string, unknown>` for flexible metadata objects that may contain arbitrary data
- Pattern: Use `?` for optional properties (e.g., avatarUrl, metadata)
- Pattern: Nest related data in structured objects rather than flat properties for better organization
- Pattern: Use descriptive interface names that indicate purpose (e.g., LearningStats, not Stats)
- Pattern: Place enum definitions before interfaces that use them for logical ordering
- Creating AGENTS.md in directories establishes patterns for future work in that module

---

## [Tue Jan 14 06:48:15 AM +07 2026] - Card #96

- Created dashboard-mock-data.ts service in src/services/ with realistic mock data for dashboard development
- Implemented getMockDashboardData() function returning complete DashboardData structure
- Implemented fetchMockDashboardData() async function with configurable delay for simulating network latency
- Mock data includes: user info, learning stats, review queue breakdown, and 5 recent activities
- Activities include all ActivityType enum variants (card_learned, streak_milestone, deck_completed, perfect_review)
- Created comprehensive tests in src/services/dashboard-mock-data.test.ts (18 tests)
- All quality gates pass: type-check, lint, and test (100 tests passing)
- Files changed: src/services/dashboard-mock-data.ts, src/services/dashboard-mock-data.test.ts, src/services/AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Pattern: Use `import type` for type-only imports to satisfy `@typescript-eslint/consistent-type-imports` rule
- Pattern: Separate value imports (enums) from type imports in the same import statement block
- Pattern: Remove explicit type annotations on function parameters with default values (e.g., `greeting = 'Good morning'` not `greeting: string = 'Good morning'`)
- Pattern: ESLint rule `@typescript-eslint/no-inferrable-types` flags trivially inferred types that should be removed
- Pattern: Mock data services should export both sync and async versions for flexibility in different use cases
- Pattern: Async mock functions should accept delay parameter (default 500ms) to simulate realistic network latency
- Pattern: Mock data should include all enum variants to enable comprehensive testing of components
- Pattern: Add type guards in tests when accessing array elements to satisfy TypeScript strict mode (e.g., `if (!activity) throw new Error(...)`)
- Pattern: For timing tests with delays, allow margin for test execution variance (e.g., expect >= 450ms for 500ms delay)
- Pattern: Use `@vitest-environment node` for pure service tests without DOM dependencies
- Pattern: Mock data should be defined as module-level constants for consistency across function calls
- Pattern: Activities should be sorted by timestamp (most recent first) to match expected UI behavior

---


## [Tue Jan 14 06:51:00 AM +07 2026] - Card #98

- Created getTimeBasedGreeting pure function in src/domain/get-time-based-greeting.ts
- Function returns appropriate greeting based on hour of day with four time ranges
- Time ranges: Morning (5-11), Afternoon (12-16), Evening (17-20), Night (21-4)
- Returns TimeBasedGreeting interface with greeting text and icon emoji
- Accepts optional Date parameter for testing, defaults to current time in production
- Created comprehensive tests in src/domain/get-time-based-greeting.test.ts (16 tests)
- Tests cover all time boundaries, edge cases, default behavior, and consistency
- All quality gates pass: type-check, lint, and test (116 tests passing)
- Files changed: src/domain/get-time-based-greeting.ts, src/domain/get-time-based-greeting.test.ts, src/domain/AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Pattern: Domain functions that depend on time should accept optional Date parameter with default `new Date()`
- Pattern: Use `Date.getHours()` to extract hour in local timezone for time-based logic
- Pattern: Export interfaces for function return types to enable type reuse across the codebase
- Pattern: Time ranges should be clearly documented in JSDoc with specific hours (e.g., "5:00 AM - 11:59 AM")
- Pattern: Test all time boundaries explicitly - start of range, middle of range, and end of range
- Pattern: Test edge cases like midnight (hour 0), early morning (hour 3-4), and late evening (hour 21-23)
- Pattern: Include consistency test to verify same input produces same output
- Pattern: Test default behavior without parameter to ensure production code path works
- Pattern: Pure domain functions should complete in milliseconds - no async, no network, no side effects
- Pattern: Use `@vitest-environment node` for domain tests to avoid unnecessary DOM overhead
- Note: Date objects use local timezone by default - ISO strings with 'Z' will be converted to local time
- Note: Including emoji icons in return types adds visual polish and works cross-platform

---

## [Wed Jan 14 06:55:09 AM +0700 2026] - Card #89

- Created ProgressBar component in src/ui/ProgressBar.tsx for visualizing completion percentage
- Component displays horizontal progress bar with smooth animation transitions when progress changes
- Implemented container + fill pattern using XStack (container) and YStack (fill bar)
- Progress value is clamped between 0-100 internally using Math.max/Math.min
- Optional height prop allows customization (defaults to 4px)
- Uses semantic theme colors: $borderColor for background, $primary for fill
- Applied 'normal' animation timing for smooth transitions
- Created comprehensive tests in src/ui/ProgressBar.test.tsx (14 tests)
- Created src/ui/AGENTS.md to document UI component patterns for future development
- All quality gates pass: type-check, lint, and test (130 tests passing)
- Files changed: src/ui/ProgressBar.tsx, src/ui/ProgressBar.test.tsx, src/ui/AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Pattern: Progress indicators use container + fill pattern with XStack (container) and YStack/XStack (fill)
- Pattern: Clamp numeric values to valid range using `Math.max(0, Math.min(100, value))` for robustness
- Pattern: Tamagui's typed width/height props don't accept percentage strings - use style prop instead
- Pattern: For percentage widths use: `style={{ width: \`${value.toString()}%\` }}` not `width={\`${value}%\`}`
- Pattern: ESLint rule `@typescript-eslint/restrict-template-expressions` requires `.toString()` on numbers in template literals
- Pattern: Avoid using string union types (`number | string`) for layout props if TypeScript rejects them - prefer single type
- Pattern: Number-only types are safer and more predictable for dimensional props (width, height)
- Pattern: Component tests don't need full DOM rendering - `React.createElement()` is sufficient for prop testing
- Pattern: Test edge cases: boundary values (0, 100), out-of-range values (negative, >100), fractional values
- Pattern: XStack container with `overflow: 'hidden'` clips fill bar to rounded corners for clean appearance
- Pattern: Apply animation to the fill element (not container) for smooth width transitions
- Pattern: Use `height: '100%'` on fill element to match container height dynamically

---

## [Wed Jan 14 06:59:09 AM +0700 2026] - Card #90

- Created HeroSection component in src/ui/dashboard/HeroSection.tsx for dashboard hero area
- Component displays personalized greeting with time-based icon, streak counter with motivational message, and primary CTA button
- Implemented as functional component that composes Card, Button, H2, Text, YStack, and XStack primitives
- Used Card with custom padding ($6) as outer container for consistent dashboard section styling
- Greeting section uses XStack with large emoji icon ($8 fontSize) and H2 heading with explicit sizing
- Streak counter uses XStack layout with fire emoji and YStack for text hierarchy
- Primary CTA button uses existing Button component with onPress callback
- Created comprehensive tests in src/ui/dashboard/HeroSection.test.tsx (21 tests)
- Tests verify component definition, all props acceptance, greeting variants, streak counts, and button interaction
- Updated src/ui/AGENTS.md with dashboard section component patterns
- All quality gates pass: type-check, lint, and test (151 tests passing)
- Files changed: src/ui/dashboard/HeroSection.tsx, src/ui/dashboard/HeroSection.test.tsx, src/ui/AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Pattern: Dashboard sections are functional components (not styled components) that compose existing UI primitives
- Pattern: Export props interface for each dashboard section component for external type reuse
- Pattern: Use Card component as outer container with custom padding (e.g., `p="$6"`) for dashboard sections
- Pattern: Dashboard sections need higher padding than default Card (`p="$6"` vs `p="$4"`) for breathing room
- Pattern: Use `gap` props on YStack/XStack containers for consistent spacing between child elements within sections
- Pattern: For prominent headings in dashboard, use H2 with explicit fontSize and fontWeight (e.g., `fontSize="$7" fontWeight="700"`)
- Pattern: Icons should use large font sizes to create visual hierarchy (e.g., `fontSize="$8"` for main icon, `fontSize="$6"` for secondary)
- Pattern: Convert numeric props to strings with `.toString()` when rendering in JSX to satisfy ESLint `restrict-template-expressions` rule
- Pattern: Use semantic color tokens: `$color` for primary text, `$secondary` for supporting/meta text
- Pattern: Dashboard sections should accept callback props (e.g., `onStartReview: () => void`) for handling user interactions
- Pattern: Use `flex={1}` on YStack content containers to allow text to fill available space in horizontal layouts
- Pattern: Test dashboard sections with React.createElement pattern - verify component definition, prop acceptance, and combined props
- Pattern: Group tests by feature area: component definition, props acceptance, greeting display, streak counter, button interaction
- Pattern: Test all time-based greeting variants (morning, afternoon, evening, night) to ensure component handles all cases
- Pattern: Test edge cases for numeric props: 0, 1, milestone values (7, 30, 100), and large values (365+)
- Note: Dashboard section tests focus on prop acceptance and component structure, not full DOM rendering or visual appearance
- Note: Removed unused `ComponentProps` import to satisfy ESLint `no-unused-vars` rule

---


## [Wed Jan 14 07:02:55 AM +0700 2026] - Card #91

- Created StatsOverviewSection component in src/ui/dashboard/StatsOverviewSection.tsx for displaying key learning metrics
- Component displays 4 metrics in a responsive 2x2 grid layout: Cards Learned, Current Streak, Total Reviews, Accuracy Rate
- Implemented section header ("Your Progress") using Text with semantic sizing and weight
- Grid uses nested XStack rows within YStack container with `flex={1}` on cells for equal widths
- Each metric wrapped in YStack with `flex={1}` to ensure proper flex behavior
- Uses MetricCard component for consistent metric display with emoji icons
- Created comprehensive tests in src/ui/dashboard/StatsOverviewSection.test.tsx (14 tests)
- Tests verify component definition, realistic stats, edge cases (zero, low, high values), milestone values, and fractional accuracy
- Updated src/ui/AGENTS.md with StatsOverviewSection patterns for grid layouts and section headers
- Updated specs/progress.txt Codebase Patterns section with Dashboard Layout Patterns
- All quality gates pass: type-check, lint, and test (165 tests passing)
- Files changed: src/ui/dashboard/StatsOverviewSection.tsx, src/ui/dashboard/StatsOverviewSection.test.tsx, src/ui/AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Pattern: Dashboard sections don't always need Card wrapper - sections that compose multiple Cards can use YStack as root
- Pattern: Section headers should use Text with `fontSize="$5"` and `fontWeight="600"` to create visual hierarchy
- Pattern: Responsive grid layouts use nested XStack (horizontal rows) within YStack with `gap="$3"` for spacing
- Pattern: Grid cells need YStack wrapper with `flex={1}` to achieve equal column widths in XStack rows
- Pattern: Emoji icons work well when passed as `<Text fontSize="$5">📚</Text>` to MetricCard icon prop
- Pattern: Convert numbers to strings with `.toString()` to satisfy `@typescript-eslint/restrict-template-expressions`
- Pattern: For percentage displays, concatenate with `%` symbol: `${stats.accuracyRate.toString()}%`
- Pattern: Remove unused imports to satisfy TypeScript strict mode (e.g., Card import was initially added but not used)
- Pattern: Grid layouts are more flexible than wrapping everything in a single Card - allows for better visual hierarchy
- Pattern: Test dashboard sections with various data scenarios: zero values, beginner stats, advanced stats, milestone values
- Pattern: Section-level components should accept typed props (e.g., `stats: LearningStats`) for type safety
- Pattern: Dashboard sections that compose existing components have simpler test patterns than interactive components

---

## [Wed Jan 14 07:07:00 AM +0700 2026] - Card #92

- Created ReviewQueueSection component in src/ui/dashboard/ReviewQueueSection.tsx for displaying review queue status
- Implemented conditional rendering: empty state when no cards due vs. queue breakdown when cards are due
- Empty state displays celebration emoji (🎉), "All caught up!" heading, and reassuring message
- Queue display shows total cards due, breakdown by category (New/Learning/Review), and "Start Reviewing" CTA button
- Created internal QueueItem helper component for displaying individual queue categories with label and count
- Implemented comprehensive tests in src/ui/dashboard/ReviewQueueSection.test.tsx (16 tests)
- Tests verify component definition, props acceptance, queue display, empty state, button interaction, and combined scenarios
- All quality gates pass: type-check, lint, and test (181 tests passing)
- Files changed: src/ui/dashboard/ReviewQueueSection.tsx, src/ui/dashboard/ReviewQueueSection.test.tsx, src/ui/AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Pattern: Text component does NOT support `textAlign` prop - wrap Text in YStack with `items="center"` for centered text alignment
- Pattern: Text component does NOT support `minWidth` prop - wrap Text in YStack with fixed `width` property for minimum width
- Pattern: Dynamic `color` prop (string type parameter) not allowed on Text component - use semantic tokens only (`$color`, `$secondary`)
- Pattern: Conditional rendering (empty vs. populated state) can be handled within single component using early return pattern
- Pattern: Helper components (like QueueItem) should be internal to file (not exported) when only used by one section component
- Pattern: Empty states should use Card wrapper with centered YStack content, celebration emoji, heading, and reassuring message
- Pattern: For fixed-width labels in lists, wrap Text in YStack with explicit `width` prop instead of using minWidth directly
- Pattern: Queue breakdown lists should use YStack with `gap="$2"` for tight vertical spacing between related items
- Pattern: Dashboard sections with Card wrapper should use consistent padding (`p="$6"`) for visual consistency across sections
- Pattern: Export both data interfaces (ReviewQueueData) and component props interfaces (ReviewQueueSectionProps) for type reuse
- Pattern: Helper components don't need separate props interfaces - inline type definitions work well for internal use
- Note: Removed `color` parameter from QueueItem helper since dynamic color strings are not allowed on Text components
- Note: All card counts use `.toString()` to convert numbers to strings in JSX for ESLint `restrict-template-expressions` compliance

---

## [Wed Jan 14 10:12:32 AM +0700 2026] - Card #93

- Created ActivityFeedSection component in src/ui/dashboard/ActivityFeedSection.tsx for displaying recent learning activities
- Component displays chronological list of activities with configurable maxVisible limit (default: 7)
- Implemented conditional rendering: empty state when no activities vs. scrollable list when activities exist
- Empty state displays seedling emoji (🌱), "No recent activity yet" heading, and encouraging message
- Activity list maps over activities array and renders ActivityItem components with key prop
- Implemented "See all activity →" link that appears when activities.length exceeds maxVisible
- Used Card wrapper with consistent padding ($6) and section header ("Recent Activity")
- Created comprehensive tests in src/ui/dashboard/ActivityFeedSection.test.tsx (25 tests)
- Tests verify component definition, prop acceptance, activities display, maxVisible limit, empty state, see all link, and combined scenarios
- Updated src/ui/AGENTS.md with ActivityFeedSection patterns for future dashboard sections
- All quality gates pass: type-check, lint, and test (206 tests passing)
- Files changed: src/ui/dashboard/ActivityFeedSection.tsx, src/ui/dashboard/ActivityFeedSection.test.tsx, src/ui/AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Pattern: Use `Array.slice(0, maxVisible)` to limit visible items without modifying the original array
- Pattern: Conditional rendering for empty vs. populated states keeps components simple and focused
- Pattern: Map over filtered array with `.map((item) => <Component key={item.id} ... />)` for list rendering
- Pattern: Empty states follow consistent pattern: Card > centered YStack > emoji + heading + message
- Pattern: "See all" links use centered YStack wrapper with primary color text ($primary) for consistency
- Pattern: Activity list renders directly in YStack without explicit gaps since ActivityItem has built-in py="$3" padding
- Pattern: Export both data interfaces (Activity) and component props interfaces (ActivityFeedSectionProps) for external reuse
- Pattern: maxVisible prop with default value pattern: `maxVisible = 7` provides flexibility without requiring prop
- Pattern: Conditional display logic: `{activities.length > maxVisible && (<link>)}` ensures link only shows when needed
- Pattern: Activity icons as ReactNode allow flexible icon types (emoji Text, SVG components, etc.)
- Pattern: Section headers consistently use Text with fontSize="$5" and fontWeight="600" across all dashboard sections
- Pattern: Removed unused imports (ScrollView) to pass TypeScript strict mode compilation
- Note: ActivityItem component handles its own vertical spacing, so parent YStack doesn't need gap prop
- Note: Tests verify all scenarios: empty arrays, single items, exact maxVisible, more than maxVisible, various timestamp formats

---

## [Wed Jan 14 10:18:26 AM +0700 2026] - Card #97

- Created useDashboardData custom React hook in src/hooks/useDashboardData.ts for managing dashboard state
- Hook implements standard async data fetching pattern with loading, data, and error states
- Uses fetchMockDashboardData service with 500ms simulated network delay
- Hook automatically fetches data on mount using useEffect with empty dependency array
- Created comprehensive tests in src/hooks/useDashboardData.test.ts (10 tests)
- Tests focus on hook definition, service layer integration, type structure, and error handling
- All quality gates pass: type-check, lint, and test (216 tests passing total)
- Created src/hooks/AGENTS.md documenting custom hook patterns for future development
- Files changed: src/hooks/useDashboardData.ts, src/hooks/useDashboardData.test.ts, src/hooks/AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Pattern: React hooks must be called within React component context - cannot be tested directly outside React
- Pattern: renderHook from @testing-library/react-native fails with "Unexpected token 'typeof'" error due to React Native Flow syntax
- Pattern: Testing strategy for hooks should focus on: (1) hook definition exists, (2) underlying service layer works, (3) type structure is correct
- Pattern: Use `void asyncFunction()` in useEffect to satisfy ESLint @typescript-eslint/no-floating-promises rule
- Pattern: Always set loading state in finally block to ensure it's updated even when errors occur
- Pattern: Clear error state on successful fetch to prevent stale errors from previous attempts
- Pattern: Clear data state when errors occur for consistency and to avoid showing stale data
- Pattern: Use `import type` for DashboardData since it's only used in type annotations
- Pattern: Type assertion with `as Error` is needed when catching errors since catch clause variable has unknown type
- Pattern: Use interface (not type alias) for test type definitions to satisfy @typescript-eslint/consistent-type-definitions
- Pattern: Hooks directory needs AGENTS.md to document patterns: state management, useEffect, error handling, testing strategies
- Note: Full hook lifecycle testing (mount, update, loading states) should be done at integration or E2E level
- Note: The hook is ready to be used in the Dashboard screen component (next card #94)

---
