# Fizzy Ralph Progress Log

## Codebase Patterns

### Tamagui Token System

- Use `createTokens()` from `tamagui` package to add custom color tokens to Tamagui config
- Custom colors should use semantic naming with standard scale increments (50-950)
- The `defaultConfig.tokens` from v4 includes `radius`, `zIndex`, `space`, and `size` but NOT `color`
- Import `createTokens` from `tamagui`, not from `@tamagui/config/v4`
- Spacing tokens use 4px base unit (1 = 4px) for consistent layout rhythm
- Typography tokens follow modular scale ratio of 1.25 (major third) starting from 16px base
- Line height tokens provide optimal readability for different text sizes
- Size tokens should match spacing tokens for consistency across components

### Tamagui Theme System

- Define separate `light` and `dark` theme objects with semantic color mappings
- Map semantic names (background, color, primary, etc.) to token values using `tokens.color.tokenName`
- Theme colors are organized into categories: backgrounds, text, borders, shadows, interactive, feedback
- Light theme uses light backgrounds with dark text; dark theme uses dark backgrounds with light text
- Include interactive state variants: base, hover, press, and focus for each color
- Tamagui transforms token references into Variable objects with `val`, `key`, `name`, `variable` properties
- Access actual color values in tests via `theme.colorName.val`

### Tamagui Animation System

- Tamagui v4 uses CSS-based animation strings: `"cubic-bezier(x1, y1, x2, y2) duration"` format
- Extend `defaultConfig.animations` by spreading it and overriding the nested `animations` property
- There is NO `createAnimations()` function - use plain object with CSS animation strings
- Access animations via `config.animations.animations` (nested structure)
- Keep standard animations under 400ms for responsive feel; use 500ms+ for celebration animations
- Use consistent easing: `cubic-bezier(0.4, 0.0, 0.2, 1)` for standard, bouncy curve for celebrations

### Tamagui Styled Components

- Use `styled(YStack, { ... })` to create reusable UI component primitives
- Some style properties (like `borderRadius`) must be in variants, not base config due to TypeScript constraints
- Styled components are objects, not functions, but work with React.createElement()
- Define interactive states at same level as base styles: `hoverStyle`, `pressStyle`, `focusStyle`
- Use variants pattern for conditional styling: `variants: { prop: { value: { styles } } }` + `defaultVariants`
- Testing styled components requires matchMedia mock in test/setup.ts for Tamagui's media query support

### Tamagui Component Props

- Use `width` and `height` for sizing (not shorthands `w`/`h`) on YStack/XStack components
- Properties like `borderRadius` cannot be used inline on YStack/XStack - must be in styled component variants
- Use `flex` property (not shorthand `f`) for flex layout properties
- Tamagui v4 has `onlyAllowShorthands: true` which restricts available inline properties
- When creating function components that wrap Tamagui primitives, avoid dynamic style props that TypeScript rejects
- Text-based styled components (like Button) don't support: `textAlign`, `lineHeight`, `borderRadius` (use variants), `userSelect`
- Use `aria-label` and `role` for accessibility instead of deprecated `accessibilityLabel` and `accessibilityHint`

---

Board ID: 03fe9lj5hgoq1bllqhi2fsabi
Started: Tue Jan 13 10:05:35 PM +07 2026

---

## [Tue Jan 13 10:14:35 PM +0700 2026] - Card #83

- Implemented spacing tokens in tamagui.config.ts using 4px base unit for consistent layout rhythm
- Defined typography tokens (fontSize and lineHeight) following modular scale ratio of 1.25 starting from 16px base
- Spacing scale includes fractional values (0.5, 1.5, 2.5) for fine control and large values (12, 16, 24, 32) for major sections
- Typography scale ranges from 12px (xs) to 64px (6xl) with semantic names for different use cases
- Line height tokens (tight, snug, normal, relaxed, loose) optimized for readability at different text sizes
- Size tokens mirror spacing tokens for consistency across width/height and spacing properties
- Created comprehensive tests in tamagui.config.test.ts to verify spacing, typography, and size tokens
- Files changed: tamagui.config.ts, tamagui.config.test.ts, specs/progress.txt

**Learnings for future iterations:**

- Spacing and size tokens should use the same values to maintain consistency between width/height and padding/margin
- The 4px base unit provides precise control while maintaining 8px grid alignment for major spacing values
- Typography scale using modular scale ratio creates harmonious visual hierarchy without arbitrary sizes
- Line heights should be optimized for each text size category: tight for headings, normal (1.5x) for body text, loose for display
- Font sizes should be documented with semantic names (xs, sm, base, lg, xl, 2xl, etc.) in comments for clarity
- Fractional spacing values (0.5, 1.5, 2.5) are essential for fine-tuning component layouts and alignment
- Token values can be accessed in tests via `.val` property (e.g., `tokens.space[4].val`) just like theme colors
- Custom tokens are merged with defaultConfig.tokens using spread operator to preserve default radius, zIndex, etc.

---

## [Tue Jan 13 10:12:10 PM +0700 2026] - Card #82

- Implemented semantic color mappings for light and dark mode themes in tamagui.config.ts
- Created comprehensive theme objects with semantic names: background, color, borders, shadows, primary, secondary, success, warning, error
- Added interactive state variants (hover, press, focus) for all color categories
- Light theme uses neutral50 background with neutral900 text; dark theme uses neutral950 background with neutral50 text
- Created comprehensive tests in tamagui.config.test.ts to verify theme configuration
- Files changed: tamagui.config.ts, tamagui.config.test.ts, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Theme objects map semantic property names to token values using `tokens.color.tokenName` syntax
- Tamagui automatically transforms token references into Variable objects with properties: `val`, `key`, `name`, `variable`
- To test theme colors, access the actual hex value via `theme.colorName.val` (e.g., `lightTheme.background.val`)
- Interactive states should follow a consistent pattern: lighter shades for hover/press in dark mode vs darker shades in light mode
- Border focus colors should use the primary color (primary500) in both themes for consistency
- Organize theme colors into logical categories with inline comments: backgrounds, text, borders, shadows, interactive, feedback
- Include transparent variants for backgrounds and text colors to support overlay and fade effects
- The `themes` object is passed to `createTamagui()` alongside `tokens` to configure both color tokens and semantic theme mappings

---

## [Tue Jan 13 10:07:50 PM +07 2026] - Card #81

- Defined custom color tokens for Language Den brand in tamagui.config.ts
- Added 5 semantic color scales: primary, success, warning, error, and neutral
- Each scale includes 11 shades (50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950)
- Implemented color tokens using `createTokens()` from tamagui package
- Files changed: tamagui.config.ts, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- The `createTokens` function must be imported from `tamagui`, not from `@tamagui/config/v4`
- The `defaultConfig.tokens` object does not include a `color` property by default - it only has `radius`, `zIndex`, `space`, and `size`
- Colors should be added to the tokens object (not themes), and themes will reference these color tokens
- Standard design system color scales use increments: 50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950 (where 500 is typically the "main" color)
- Semantic color naming (primary, success, warning, error, neutral) is preferred over generic names (blue, green, yellow, red, gray)
- Custom tokens automatically get TypeScript typing through the `TamaguiCustomConfig` module declaration

---
## [Mon Jan 13 10:18:45 PM +07 2026] - Card #84

- Implemented animation timing tokens in tamagui.config.ts for consistent motion across the app
- Added four animation presets: quick (150ms), normal (250ms), slow (400ms), and celebration (500ms)
- Used CSS-based animation strings with cubic-bezier easing functions
- Quick/normal/slow use standard easeInOut curve; celebration uses bouncy curve for achievements
- Extended defaultConfig.animations by spreading and overriding the nested animations property
- Created comprehensive tests in tamagui.config.test.ts to verify animation timing and easing
- All animation tests pass including duration verification and easing function validation
- Files changed: tamagui.config.ts, tamagui.config.test.ts, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Tamagui v4 does NOT have a `createAnimations()` function - the card's example code was incorrect
- Animations in Tamagui v4 are CSS strings in format: `"cubic-bezier(x1, y1, x2, y2) duration"`
- Must extend `defaultConfig.animations` by spreading entire object and overriding nested `animations` property
- Animation structure is nested: access via `config.animations.animations.quick`, not `config.animations.quick`
- Testing animations requires parsing CSS strings with regex: `/(\d+)ms/.exec(animationString)`
- Use optional chaining (`?.[1]`) when accessing regex match groups to satisfy TypeScript strict mode
- ESLint prefers `RegExp.exec()` over `String.match()` for performance and consistency
- Standard animations kept under 400ms for responsive feel; celebration animation can be longer (500ms+)
- Reduced motion support should be implemented in components using `useReducedMotion()` hook from React Native
- The cubic-bezier values define the animation curve: (0.4, 0.0, 0.2, 1) = easeInOut, (0.68, -0.55, 0.265, 1.55) = bouncy

---

## [Mon Jan 13 10:24:45 PM +07 2026] - Card #85

- Created reusable Card component in src/ui/Card.tsx using Tamagui's styled(YStack)
- Card provides consistent visual container with elevation, rounded corners, and theme-aware styling
- Implemented interactive states: hoverStyle, pressStyle, and focusStyle for accessibility
- Used variants system for borderRadius styling (borderRadius in base config causes TypeScript errors)
- Created comprehensive tests in src/ui/Card.test.tsx verifying component creation and prop acceptance
- Added matchMedia mock to test/setup.ts for Tamagui components that use media queries
- All quality gates pass: type-check, lint, and test (43 tests passing)
- Files changed: src/ui/Card.tsx, src/ui/Card.test.tsx, test/setup.ts, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Tamagui styled components are objects (not plain functions), but work with React.createElement()
- Some style properties like borderRadius must be defined in variants, not in base style config
- The `borderWidth` property doesn't have a shorthand (`bw`) that TypeScript recognizes - use full name
- Interactive states (hoverStyle, pressStyle, focusStyle) should be defined at the same level as base styles
- matchMedia mock is required in test/setup.ts for Tamagui components that use @tamagui/select or similar
- Use `globalThis as any` with appropriate eslint-disable comments to mock window.matchMedia in strict mode
- Variants pattern: `variants: { propName: { value: { styles } } }` with `defaultVariants: { propName: value }`
- Styled component tests don't need full DOM rendering - React.createElement() is sufficient for prop testing
- The animation prop accepts token names like 'quick', 'normal', 'slow' defined in tamagui.config.ts
- Shadow props work cross-platform: shadowColor, shadowOffset, shadowOpacity, shadowRadius (iOS/web) + elevation (Android)

---

## [Mon Jan 13 10:28:45 PM +07 2026] - Card #86

- Created MetricCard component in src/ui/MetricCard.tsx for displaying dashboard metrics
- Component displays label, prominent value, optional icon, and optional trend indicator
- Built on top of Card component for consistent elevation and styling
- Implemented trend indicator with up/down arrows and color-coded text (success/error)
- Icon container uses primary color background with fixed size (40px x 40px)
- Created comprehensive tests in src/ui/MetricCard.test.tsx (11 tests)
- All quality gates pass: type-check, lint, and test (54 tests passing)
- Files changed: src/ui/MetricCard.tsx, src/ui/MetricCard.test.tsx, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Tamagui v4 requires `width` and `height` properties (not shorthands `w`/`h`) for YStack/XStack sizing
- The `flex` property must be used instead of shorthand `f` for flex layout
- Properties like `borderRadius` cannot be used inline on YStack/XStack - they must be in styled component variants
- Tamagui v4 has `onlyAllowShorthands: true` setting which restricts which properties can be used directly in JSX
- Dynamic color props (e.g., `color?: string`) cannot be passed directly to `bg` prop due to TypeScript type constraints
- When TypeScript rejects a style prop, either use a fixed token value or create a styled component with variants
- For simple icon containers, omitting borderRadius entirely is acceptable if rounded corners aren't critical
- Function components wrapping Tamagui primitives should avoid dynamic style props that TypeScript cannot validate
- MetricCard test pattern: verify all props render correctly, test optional props independently, test combined props
- Component should use semantic color tokens ($primary, $success, $error) rather than hardcoded colors

---

## [Tue Jan 13 10:33:00 PM +0700 2026] - Card #87

- Created Button component in src/ui/Button.tsx using Tamagui's styled(Text)
- Button provides accessible, cross-platform button with primary styling and press feedback
- Implemented interactive states: hoverStyle, pressStyle, and focusStyle for accessibility
- Used variants system for borderRadius styling (must be in variants, not base config)
- Shadow properties work cross-platform: shadowColor, shadowOffset, shadowOpacity, shadowRadius + elevation
- Created comprehensive tests in src/ui/Button.test.tsx (15 tests)
- All quality gates pass: type-check, lint, and test (69 tests passing)
- Files changed: src/ui/Button.tsx, src/ui/Button.test.tsx, AGENTS.md, specs/progress.txt

**Learnings for future iterations:**

- Text-based styled components have more restrictions than YStack/XStack components
- Properties that don't work on styled(Text): textAlign, lineHeight, borderRadius (base), userSelect
- borderRadius must be defined in variants for Text-based components, just like YStack/XStack
- Button components work well when styled from Text with proper padding, background, and interactive states
- Use aria-label instead of deprecated accessibilityLabel for accessibility support
- Use role prop instead of deprecated accessibilityHint for accessibility
- Text components automatically handle typography styling (fontSize, fontWeight, color)
- cursor: 'pointer' is allowed on Text components for proper web cursor behavior
- Scale transform in pressStyle (scale: 0.98) provides nice tactile feedback on press
- All styled components (Text, YStack, XStack) share the same pattern: variants with defaultVariants

---
