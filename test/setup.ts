/**
 * Vitest setup file
 *
 * This file runs before each test file to configure the test environment.
 * For pure TypeScript tests (domain layer), minimal setup is needed.
 * React component tests will extend this setup in a future card.
 */

// Global test environment setup can go here
// Currently empty as pure TypeScript tests don't need special setup

// Example: Custom matchers, global mocks, or environment configuration
// could be added here as needed
