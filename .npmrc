# Enforce pnpm as the package manager
engine-strict=true

# Shamefully-hoist to fix React Native Metro bundler issues with pnpm
# This ensures dependencies are hoisted to node_modules root
node-linker=hoisted
